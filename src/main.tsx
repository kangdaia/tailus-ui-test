import React from 'react'
import ReactDOM from 'react-dom/client'
import { HeroSection1 } from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HeroSection1 />
  </React.StrictMode>,
)
